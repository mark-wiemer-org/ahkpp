; [Issue #255](https://github.com/mark-wiemer-org/ahkpp/issues/255)
{
    if
        if
            code
    loop
        code
    code

    if
        loop
            code
    else
        code
    code

    loop
        if
            loop
                loop
                    code
        else
            code
    code

    Loop
        Loop
            if {
                code
            } else
                code
    code

    Loop
        if
            code
        else
            code
    code

    if {
        code
    } else
        loop {
            code
        }
    code

    loop {
        if {
            code
        } else {
            code
        }
        code
    }
}
