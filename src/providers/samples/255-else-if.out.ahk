; [Issue #255](https://github.com/mark-wiemer-org/ahkpp/issues/255)
{
    if
        code
    else if
        code
    else
        code
    code

    if
        code
    else if {
        code
    }
    else
        code
    code

    if {
        code
    } else if {
        code
    }
    else
        code
    code

    if
        code
    else if
    {
        code
    }
    else
        code
    code
}
