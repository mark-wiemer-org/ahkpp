; [Issue #255](https://github.com/mark-wiemer-org/ahkpp/issues/255)
label:
    if {
        code
    } else
        code
return

label:
    if {
        code
    }
label:
    code
return

label:
    if {
        code
    } else
        code
label:
    code
return
