#Requires AutoHotkey v2.0
#SingleInstance

;; General
; showOutputView
; always: shows on start
; never: never shows
; works for both running and debugging

; always+run
; always+debug
; never+run
; never+debug
x := 1